<%= render "nav"%>

<!-- bloc-2 -->
<div class="bloc l-bloc bgc-white" id="bloc-2">
  <div class="container bloc-lg">
    <div class="row voffset">
      <div class="col-sm-3">
        <%= @articles.each do |p| %>
          <%= image_tag "placeholder-image.png", class: "img-responsive" %>
          <h3 class="mg-md">
            <%= p.title %>
          </h3>
          <p>
            <%= p.body %>
          </p>
          <% end %>
      </div>
    </div>
  </div>
</div>
<!-- bloc-2 END -->

<%= render "footer" %>